language: rust
cache: cargo
rust:
  - stable
  - beta
  - nightly
jobs:
  allow_failures:
    - rust: nightly
  fast_finish: true
script:
  - rustc --version && cargo --version
  - cargo build --verbose
  - cargo test --verbose
